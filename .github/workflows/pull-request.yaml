# $schema: http://json.schemastore.org/github-workflow.json
name: pull-request

on:
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify-github-workflows:
    uses: harmelodic/workflows/.github/workflows/verify-github-workflows.yaml@a3fbc4cc16ed5ad102d2defa263555327a2b17b3

  verify-terraform-infrastructure:
    uses: harmelodic/workflows/.github/workflows/verify-terraform.yaml@a3fbc4cc16ed5ad102d2defa263555327a2b17b3
    with:
      directory: infrastructure

# TODO: Verify `gitops-argo-cd` manifests.
# TODO: Verify skaffold file
# TODO: Verify `manifests` kustomization files
# TODO: Verify rendered `manifests` manifests.
